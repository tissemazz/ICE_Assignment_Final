export declare type Config = {
    readonly [key: string]: number;
};
export declare type Breakpoint<C extends Config> = {
    breakpoint: keyof C;
    maxWidth?: number;
    minWidth: C[keyof C];
};
declare type MediaQuery<C extends Config> = {
    breakpoint: keyof C;
    maxWidth?: number;
    minWidth: C[keyof C];
    query: string;
};
/**
 * Create media query objects
 * @param breakpoints the list of configured breakpoint names and their pixel values
 */
export declare const createMediaQueries: (breakpoints: Config) => MediaQuery<Config>[];
export default function useBreakpoint<C extends Config>(config: C): Breakpoint<C> | undefined;
export default function useBreakpoint<C extends Config>(config: C, defaultBreakpoint: keyof C): Breakpoint<C>;
export {};
